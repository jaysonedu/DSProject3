# Copy to .env.local (same folder) and restart `pnpm dev-fe`.
#
# Local API: from repo DSProject3/study-server run `npm start` (port 8787).
# Production: set this to your deployed API, e.g. https://your-api.onrender.com/submit
#
# Use your LAN IP instead of localhost if phones on Wi‑Fi need to reach your PC.
VITE_DS3_COLLECT_URL=http://localhost:8787/submit
