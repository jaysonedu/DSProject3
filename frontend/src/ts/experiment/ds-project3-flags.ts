/**
 * Dependency-free switch for the DS Project 3 study UI.
 * Used by notifications and ready bootstrap so we avoid import cycles with ds-project3-study.ts.
 */
export const DS_PROJECT3_STUDY_ENABLED = true;
